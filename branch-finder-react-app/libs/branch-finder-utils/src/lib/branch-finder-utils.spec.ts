import { branchFinderUtils } from './branch-finder-utils';

describe('branchFinderUtils', () => {
  it('should work', () => {
    expect(branchFinderUtils()).toEqual('branch-finder-utils');
  });
});
