import { branchFinderSchemas } from './branch-finder-schemas';

describe('branchFinderSchemas', () => {
  it('should work', () => {
    expect(branchFinderSchemas()).toEqual('branch-finder-schemas');
  });
});
